name: energy-algorithms
channels:
 - conda-forge
 - defaults
dependencies:
 # Runtime
 - python>=3.11
 - numpy>=2.0
 - scipy>=1.12
 - pandas>=2.0
 - matplotlib>=3.8
 - pulp>=3.0
 - yfinance>=0.2.30
 - requests>=2.28

 # Dev
 - pytest>=7.0
 - pytest-cov>=4.0
 - hypothesis>=6.0
 - mypy>=1.15
 - ruff>=0.8
 - pre-commit

 # Docs
 - sphinx>=7.0
 - pip

 # Pip-only deps (editable install)
 - pip:
 - -e ".[live]"
